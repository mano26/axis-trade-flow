# =============================================================================
# Services Package
# =============================================================================
# Business logic layer. Services are stateless modules that operate on
# models and return results. They do not import Flask request/session
# objects directly (those are passed in by the routes).
# =============================================================================
